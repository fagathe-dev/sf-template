<nav class="navbar navbar-expand-lg bg-body-white shadow-sm">
  <div class="container-fluid">
    <a class="navbar-brand" href=# onclick="event.preventDefault();">
      <img class="app-logo" src="{{ asset('/images/logo.png') }}" alt="Logo du site" title="Logo du site" srcset="{{ asset('/images/logo.png') }}">
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNavDropdown">
      <ul class="navbar-nav">
        {% if app.user %}
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="{{ url('app_logout') }}">Me déconnecter</a>
          </li>
        {% else %}
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="{{ url('app_login') }}">Me connecter</a>
          </li>
          <li class="nav-item">
            <a class="btn btn-primary btn-sm" aria-current="page" href="{{ url('app_registration_index') }}">M'inscrire</a>
          </li>
        {% endif %}
      </ul>
    </div>
  </div>
</nav>