<nav class="navbar navbar-expand-lg navbar-landing bg-white shadow" id="navbar">
  <div class="container">
    <a class="navbar-brand" href="{{ url('app_home_index') }}">
      <img src="{{ asset('images/logo.png') }}" class="card-logo card-logo-dark" alt="logo dark" height="32">
    </a>
    <button class="navbar-toggler py-0 fs-20 text-body" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <i class="mdi mdi-menu"></i>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mx-auto mt-2 mt-lg-0" id="navbar-example">
        <li class="nav-item">
          <a class="nav-link fs-15" href="{{ url('app_home_index') }}">Accueil</a>
        </li>
      </ul>

      <div class="">
        {% if app.user %}
          <a class="btn btn-ghost-primary fw-medium text-decoration-none text-dark" href="{{ url('app_logout') }}">Me déconnecter</a>
        {% else %}
          <a class="btn btn-ghost-primary fw-medium text-decoration-none text-dark" href="{{ url('app_registration_index') }}">M'inscrire</a>
          <a class="btn btn-soft-primary" href="{{ url('app_login') }}">Me connecter</a>
        {% endif %}
      </div>
    </div>

  </div>
</nav>
<!-- end navbar -->